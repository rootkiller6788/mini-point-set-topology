/-
# Harvard Math 131 Benchmark — MiniConnectedness

Problems inspired by Harvard's topology course (Math 131) covering
connectedness and related topics.
-/

import MiniConnectedness

open MiniConnectedness

#eval "══ HARVARD MATH 131 BENCHMARK ══"

/-! ### Problem 1: Connectedness of GL(n,R) -/
#eval "Prove GL(n,R) has two connected components. Show GL(n,C) is connected."

/-! ### Problem 2: Fixed Point Property -/
#eval "Prove every continuous map f: [0,1] → [0,1] has a fixed point using connectedness."

/-! ### Problem 3: Intermediate Value for Metric Spaces -/
#eval "Let X be connected and f: X → R continuous. If f(a) < c < f(b), then ∃ x with f(x) = c."

/-! ### Problem 4: Path Components in SO(3) -/
#eval "Show SO(3) is path-connected. Describe the path components of O(3)."

/-! ### Problem 5: Connectedness and Separation -/
#eval "If A and B are separations of X, then X is disconnected. Prove the converse."

#eval "══ HARVARD BENCHMARK COMPLETE ══"
